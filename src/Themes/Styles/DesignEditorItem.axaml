<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls1="clr-namespace:ArxisStudio.Controls"
                    xmlns:arxisStudio="clr-namespace:ArxisStudio">

    <SolidColorBrush x:Key="DesignEditorItem.HighlightColor" Color="DodgerBlue" Opacity="0.2" />

    <ControlTheme x:Key="{x:Type arxisStudio:DesignEditorItem}" TargetType="arxisStudio:DesignEditorItem">
        <Setter Property="Background" Value="Transparent" />

        <Setter Property="BorderBrush" Value="DodgerBlue" />

        <Setter Property="BorderThickness" Value="0" />

        <Setter Property="SelectedBorderThickness" Value="1" />
        <Setter Property="SelectedBrush" Value="#0099FF" />

        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ClipToBounds" Value="False" />

        <Setter Property="UseLayoutRounding" Value="True" />

        <Setter Property="Template">
            <ControlTemplate>
                <Grid ClipToBounds="False" Background="Transparent">

                    <Border Name="PART_Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter Name="PART_ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Border>

                    <Border Name="PART_HoverBorder"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            IsHitTestVisible="False"
                            Opacity="0" />

                    <controls1:ResizeAdorner Name="PART_Resizer"
                                            AdornerBrush="{TemplateBinding SelectedBrush}"
                                            HandleSize="8"
                                            IsVisible="False"
                                            IsHitTestVisible="True" />

                    <StackPanel Name="PART_Toolbar"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Margin="0,-28,0,0"
                                Spacing="2"
                                Opacity="0"
                                IsHitTestVisible="False">
                    </StackPanel>

                </Grid>
            </ControlTemplate>
        </Setter>

        <Style Selector="^:selected">
            <Setter Property="BorderBrush" Value="{Binding SelectedBrush, RelativeSource={RelativeSource Self}}" />
            <Setter Property="BorderThickness" Value="{Binding SelectedBorderThickness, RelativeSource={RelativeSource Self}}" />
            <Setter Property="Margin" Value="{Binding SelectedMargin, RelativeSource={RelativeSource Self}}" />

            <Setter Property="ZIndex" Value="99" />

            <Style Selector="^ /template/ controls1|ResizeAdorner#PART_Resizer">
                <Setter Property="IsVisible" Value="True"/>
            </Style>
            <Style Selector="^ /template/ StackPanel#PART_Toolbar">
                <Setter Property="Opacity" Value="1"/>
                <Setter Property="IsHitTestVisible" Value="True"/>
            </Style>
        </Style>

        <Style Selector="^:pointerover:not(:selected) /template/ Border#PART_HoverBorder">
             <Setter Property="Opacity" Value="1"/>
        </Style>

    </ControlTheme>
</ResourceDictionary>
